export enum PaginationControlEnum {
	// the results will be the list, or appended (like no strategy)
	Normal,
	// put the results on the start of the list
	Prepend,
	// put the results on the end of the list
	Append,
}
